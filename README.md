This is a simple todo app created with next js as frontend , firebase as authentication backend , prisma ORM and neon db as database

## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

